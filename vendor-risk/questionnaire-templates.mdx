---
title: "Questionnaire Templates"
description: "Library of typed, tiered, scoreable questionnaire templates that drive assessments and deterministic risk scoring"
---

A library of reusable security questionnaires, each tagged to one or more vendor tiers. Templates can be built from scratch, seeded from a built-in security questionnaire, or extracted from an Excel workbook you upload — including 450-plus-question, multi-sheet workbooks. Each template defines its questions' answer types, conditional sub-questions, evidence policy, and the deterministic scoring model used to compute the [assessment risk score](/concepts/risk-scoring).

<Frame>
  <img src="/images/screenshots/questionnaire-templates.png" alt="Questionnaires page with template list" />
</Frame>

## What it does

- Stores multiple templates per tenant. Each template is named, tier-tagged (Critical, High, Medium, Low, or All Tiers), described, and optionally carries a stable **plan slug** that engagement assessment plans reference.
- Extracts questions from an uploaded Excel file using model-driven extraction — one call per sheet, so large workbooks import reliably and atomically — or seeds from a built-in questionnaire.
- Supports four answer types — **text, yes/no, single-select, multi-select** — with per-option answer choices, customer-authored vendor guidance, and per-option numeric scores.
- Nests **sub-questions** one level under a parent, shown to the vendor only when the parent's answer matches a condition, and evaluated with the parent's question and answer as context.
- Sets the template's **evidence policy**: evidence required (default), answers sufficient (attestation), or audited-reports sufficient — plus an optional cross-answer consistency check that runs after vendor submission.
- Owns the **scoring configuration** — question weights, score ranges and direction, per-option scores, and rating bands — on a dedicated Scoring page with a best-case/worst-case preview.
- **Archives** templates instead of deleting them; archived templates leave the active list and tier resolution but keep existing assessments working, and can be restored.
- Exports any template to Excel (one worksheet per category) in a format that round-trips back through upload.

## Building a template

### Question sources

- **Excel upload** (`.xlsx` / `.xls`). Extraction runs one model call per sheet with automatic retry and row-splitting for oversized sheets, so large multi-sheet workbooks import without truncation. The import is atomic — if any sheet fails, nothing is imported and the failing sheet is named. Extraction picks up answer types and options, guidance, dotted-number parent/child structure, and show-when conditions.
- **Built-in seed** — a starter security questionnaire.
- **Manual** — one question at a time, with drag-to-nest for sub-questions.

### Per-question fields

- **Question text** and **question number** — numbers are assigned automatically per category (sub-questions as `3.1`, `3.2`, …); a question whose text already carries an authored tag (e.g., `A. `, `4.2 `) keeps that tag.
- **Answer type** — Text, Yes/No, Single-select, or Multi-select. Select types take 2–12 options.
- **Expected answer** (text questions) or **answer options** (choice questions).
- **Guidance** — optional customer-authored help shown to the vendor on the portal.
- **Criticality** — Critical / High / Medium / Low. Question weight defaults from criticality (2.0 / 1.5 / 1.0 / 0.5) and can be overridden on the Scoring page.
- **Sub-question settings** — the parent to group under and the parent answers that reveal the question. Only yes/no and select parents can gate sub-questions; a hidden sub-question is excluded from the vendor's completion stats and submit gate, and reappears with its state intact if the parent's answer changes.

### Evidence policy (evaluation mode)

Set per template and snapshotted onto each assessment at creation, so changing the template later never changes an in-flight assessment:

| Mode | Behavior |
|---|---|
| **Evidence required** (default) | Every answer is evaluated against uploaded evidence |
| **Answers sufficient** | Attestation mode — a matching answer verifies as-is; evidence is requested only where the question explicitly demands a document |
| **Audited reports sufficient** | A specific statement in a submitted SOC 2 or VAPT report counts as primary evidence for practice questions; questions demanding a deliverable still require it |

A separate **consistency check** toggle runs a cross-answer contradiction sweep after the vendor submits.

### Scoring configuration

The **Scoring** page (from the template header) is the single owner of everything the [assessment risk score](/concepts/risk-scoring) reads: per-option scores, each question's score range and direction, question weights, and the rating band ranges for each top-level category. A preview simulator shows the score a best-case and worst-case response set would produce. Weights shown elsewhere in the template are read-only.

### Document requirements

A **Documents** tab defines the document types the assessment requests from the vendor (SOC 2, ISO 27001, VAPT, …), each with a validity window in months and a required/optional flag.

## Plan slugs

A template can carry a short stable **slug** (e.g., `pq-standard`). Engagement assessment plans reference templates by slug, not by name, so renaming a template never breaks a plan. Changing or archiving a slug that a plan still references is blocked with an "in use" error.

## Archival

Templates are archived, not deleted. Archived templates:

- disappear from the active list and from tier resolution when assessments launch,
- keep every existing assessment that references them working,
- sit in a collapsed "Archived" section and can be **restored** at any time.

Archiving is blocked while an engagement assessment plan references the template's slug.

## How templates are used at assessment time

- **Launch.** An engagement's classifier selects questionnaires by plan slug; a tenant-level launch mode decides whether multiple questionnaires run as **parallel** assessment tracks or **merge** into one assessment. A merged assessment records which template (and which category subset) each block of questions came from, why it was added, and by whom; one questionnaire is primary and governs the document checklist and evidence policy.
- **Scoring.** Each questionnaire scores against its own template's weights and bands; a merged assessment's score is the average across its questionnaires.
- **Import.** When a vendor returns a filled workbook by email instead of the portal, [questionnaire import](/vendor-risk/assessments) matches it against the template library and lands the answers on a standard assessment.

## Limitations

- No template duplication/clone and no version history.
- Replacing a template's questions via re-upload is destructive and irreversible; the action requires an explicit confirmation.
- The question filter dropdown on the template detail still filters by Text and Yes/No only; select-type questions appear under All.
- Scoring changes never re-score completed assessments.
