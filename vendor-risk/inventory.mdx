---
title: "Vendor Inventory"
description: "Catalog of third-party vendors with risk bands, contract metadata, and per-vendor profile drill-down"
---

The vendor inventory is the central list of every third-party vendor in your tenant. Each row shows the current risk band, tier, contract value, renewal date, and last assessment date. Clicking a vendor opens that vendor's profile — the canonical drill-down with full risk detail, per-analyzer breakdowns, threat model, recommended controls, and downloadable risk report.

<Frame>
  <img src="/images/screenshots/vendor-inventory.png" alt="Vendor Inventory list" />
</Frame>

## What it does

- Lists every vendor in the tenant, filterable by tier, status, risk band, or free-text search across name, category, and description.
- Shows summary counts at the top: total vendors, critical-band vendors, active vendors, and pending vendors.
- Tracks vendor lifecycle status: **active**, **pending** (documents uploaded but extraction in progress, displayed as `Processing`), **offboarding**, **terminated**.
- Sorts every column ascending or descending, including risk band and score.
- Marks documents sourced from a public Trust Center with a shield icon.
- Provides a delete action per vendor with a confirmation step.

## Inputs

Vendors enter the inventory through the **Add Vendor** action, which opens a two-step dialog:

<Steps>
  <Step title="Identify the vendor">
    Select an existing vendor from the combobox or enter a new vendor name. Selecting an existing vendor attaches the uploaded documents to that vendor; a new name creates a new vendor record.
  </Step>
  <Step title="Upload supporting documents">
    Drag-and-drop one or more files, or browse to attach them. Supported types: PDF, DOCX, TXT, XLSX, up to 10 MB per file. Password-protected files are accepted with a password field.
  </Step>
</Steps>

Typical documents uploaded at intake:

- Vendor contracts and order forms
- SOC 2 Type II reports
- ISO 27001 certificates and Statements of Applicability
- VAPT (penetration test) reports
- Privacy policies
- Completed security questionnaires
- Architecture diagrams (drives threat modeling)

The dialog closes immediately on submit. The new vendor appears in the inventory in the `Processing` state while extraction runs in the background.

### Add from a URL

Instead of uploading files, paste a **URL** — from the Add Vendor flow, or from an existing vendor's **Documents** tab. The URL can be a direct document link or a page hosting several documents, such as a public trust center or a support-portal attachment page. The platform fetches what it finds — up to 25 documents per run, 50 MB per file — and each fetched file enters the same pipeline as an upload: classification, analysis, threat assessment.

- **The vendor name is optional** on Add Vendor. Left blank, the platform identifies the vendor from the first fetched document and attaches the rest to it.
- **Recognized trust centers hand off.** If the page is a SafeBase or Conveyor trust center, the run saves it as the vendor's Trust Center, pulls its public documents through the standard trust-center machinery, and reports how many documents are NDA-gated (fetch those via **Fetch from Trust Center** once access is granted).
- **Every run produces a per-document report** — what was fetched, what was skipped, and why — visible while the run progresses.

Re-running the same URL re-analyzes nothing: already-ingested documents are recognized and skipped.

A PDF that wraps other files — a cover letter carrying a trust package of embedded reports and spreadsheets — is unpacked automatically: each embedded file is extracted and registered as a document of its own, classified and analyzed independently. One portfolio upload lands as the wrapper plus every attachment on the vendor's Documents tab. This applies on every ingestion path (upload dialogs, vendor portal, trust center, SharePoint, OneDrive, Dropbox).

## Outputs

For each vendor, the inventory shows:

| Column | Meaning |
| --- | --- |
| Vendor Name | Links to the vendor profile |
| Status | Active / Processing / Offboarding / Terminated |
| Tier | Critical / High / Medium / Low — drives review cadence (see [Risk Scoring](/concepts/risk-scoring)) |
| Category | Vendor category (e.g., CRM, Cloud Storage) |
| Annual Value | Current annual contract value, in USD |
| Renewal Date | Next contract renewal date |
| Last Assessed | Date of the most recent risk assessment |
| Risk Band | One of six bands with score (e.g., `High · 65`) |

### Vendor profile

Clicking a vendor name opens that vendor's profile.

<Frame caption="Vendor profile — overview tab">
  <img src="/images/screenshots/vendor-profile-vymo.png" alt="Vendor profile drill-down" />
</Frame>

Tabs:

- **Overview** — validated risk band + coverage, per-analyzer contribution (SOC 2, ISO 27001, VAPT, Privacy Policy, Questionnaire, Contract), summary chips.
- **Documents** — every document uploaded or auto-fetched for this vendor. Documents sourced from a public Trust Center carry a shield icon. Failed or rejected documents can be reprocessed or soft-deleted; reuploading a previously deleted document restores its prior analysis.
- **Risk Report** — per-analyzer detail pages (SOC 2 Summary, ISO 27001 Summary, VAPT Findings & OWASP matrix, Privacy Policy Metrics, Overall Risk Report). Downloadable as a single vendor risk report. A document uploaded as a VAPT report that turns out not to be one — a program summary, a policy, an attestation letter — gets an explanatory verdict card stating what it is and which signals of a real test report are missing, with upload and regenerate actions, rather than an empty analysis.
- **Threat Model** — the vendor's threats ranked by computed risk band, each expanding in place to its DREAD dimension detail and provenance. Since August 2026 this comes from the entity-level threat assessment: one assessment built from **all** of the vendor's documents, run automatically when the vendor is created and as documents arrive — an architecture diagram is no longer required.
- **Controls** — recommended controls in three lists: **buyer-side** controls (each with a "check via" verification pointer and the threats it addresses), **vendor-side** controls, and **SOC 2 complementary user-entity controls**.
- **Vendor Questions** — the open questions and claims needing evidence that the assessment could not establish, each showing which threat scores currently assume an answer.
- **Evidence** — the security fact sheet: every fact pulled from the vendor's documents, grouped by subject, each with an evidence-grade chip and its source document. Searchable.

<Frame caption="The Threat Model tab: threats ranked by risk, with the STRIDE categories each one maps to and the answers each score depends on.">
  <img src="/images/screenshots/vendor-profile-threat-model.png" alt="Vendor profile Threat Model tab with ranked threats" />
</Frame>

<Frame caption="The Evidence tab: the vendor's security fact sheet, graded by what actually establishes each fact — independently tested, auditor attested, vendor claimed, or not yet established.">
  <img src="/images/screenshots/vendor-profile-evidence.png" alt="Vendor profile Evidence tab with graded facts" />
</Frame>
- **4th Parties** — sub-processors declared by the vendor (behind feature flag).
- **Findings** — findings raised on this vendor (link to the [Findings](/findings) page).

## How it works

The inventory loads vendors for the current tenant from the backend. Risk bands and last-assessed dates come from a per-vendor risk summary that the platform maintains as documents are processed.

Risk bands reflect evidence across all document types uploaded for that vendor — security reports, contracts, questionnaires, VAPT, privacy policies, and threat model output. Bands update as new evidence is processed, as documents are reprocessed, and as the platform re-evaluates standards (see [Vendor Contract Standards](/vendor-risk/contract-standards)).

New vendors created via **Add Vendor** are visible immediately in the `Processing` state. Once document extraction finishes, the status transitions to `Active` and the risk band populates.

Documents can also enter the inventory automatically via **Trust Center extraction** — for vendors whose public Trust Centers are supported (SafeBase, Conveyor), the platform can pull public evidence directly. Trust Center-sourced documents are marked with a shield icon.

## Limitations

- Risk Band and Last Assessed are blank until at least one risk-bearing document has been processed for the vendor.
- Annual Value and Renewal Date populate from contract documents. If no contract has been uploaded, or the contract did not contain those fields, those columns remain empty.
- A full threat assessment takes up to an hour or so after documents land; the Threat Model, Controls, Vendor Questions, and Evidence tabs show an in-progress notice and fill in without a reload. A vendor with no documents gets no assessment.
- URL ingest fetches up to 25 documents per run at 50 MB per file. Pages that require a login or NDA cannot be fetched — for gated trust centers, the run reports the gated count and the documents come through the Trust Center flow once access exists.
- 4th-Party sub-processor visibility is behind a feature flag; enable per tenant.
- Vendor deletion is irreversible and removes all attached documents. There is no soft-delete or restore for vendors (individual documents do support soft delete).
