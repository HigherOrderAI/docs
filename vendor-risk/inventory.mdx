---
title: "Vendor Inventory"
description: "Catalog of third-party vendors with risk grades, contract metadata, and per-vendor profile drill-down"
---

The vendor inventory is the central list of every third-party vendor in your tenant. Each row shows the current risk grade, tier, contract value, renewal date, and last assessment date. Clicking a vendor opens that vendor's profile, which contains the full risk scoring detail and a downloadable risk report.

## What it does

- Lists every vendor in the tenant, filterable by tier, status, risk grade, or free-text search across name, category, and description.
- Shows summary counts at the top: total vendors, critical-tier vendors, active vendors, and pending vendors.
- Tracks vendor lifecycle status: **active**, **pending** (documents uploaded but extraction in progress, displayed as `Processing`), **offboarding**, **terminated**.
- Sorts every column ascending or descending, including risk grade.
- Provides a delete action per vendor with a confirmation step.

## Inputs

Vendors enter the inventory through the **Add Vendor** action, which opens a two-step dialog:

<Steps>
  <Step title="Identify the vendor">
    Select an existing vendor from the combobox or enter a new vendor name. Selecting an existing vendor attaches the uploaded documents to that vendor; a new name creates a new vendor record.
  </Step>
  <Step title="Upload supporting documents">
    Drag-and-drop one or more files, or browse to attach them. Supported types: PDF, DOCX, TXT, XLSX. Password-protected files are accepted with a password field.
  </Step>
</Steps>

Typical documents uploaded at intake:

- Vendor contracts and order forms
- SOC 2 Type II reports
- ISO 27001 certificates and Statements of Applicability
- VAPT (penetration test) reports
- Privacy policies
- Completed security questionnaires

The dialog closes immediately on submit. The new vendor appears in the inventory in the `Processing` state while extraction runs in the background.

## Outputs

For each vendor, the inventory shows:

| Column | Meaning |
| --- | --- |
| Vendor Name | Links to the vendor profile |
| Status | Active / Processing / Offboarding / Terminated |
| Tier | Critical / High / Medium / Low — drives review cadence |
| Category | Vendor category (e.g., CRM, Cloud Storage) |
| Annual Value | Current annual contract value, in USD |
| Renewal Date | Next contract renewal date |
| Last Assessed | Date of the most recent risk assessment |
| Risk Grade | Letter grade A through F, computed across all evidence |

### Vendor profile

Clicking a vendor name opens that vendor's profile, which presents the current risk grade, the supporting risk-scoring detail, and a downloadable risk report. The profile is the canonical drill-down for any vendor in the inventory.

## How it works

The inventory loads vendors for the current tenant from the backend. Risk grades and last-assessed dates come from a per-vendor risk summary that the platform maintains as documents are processed.

Risk grades reflect evidence across all document types uploaded for that vendor — security reports, contracts, questionnaires, VAPT, privacy policies. Grades update as new evidence is processed and as the platform re-evaluates standards (see [Vendor Contract](/vendor-risk/contract-standards) for how Golden Standards drive contract scoring).

New vendors created via **Add Vendor** are visible immediately in the `Processing` state. Once document extraction finishes, the status transitions to `Active` and the risk grade populates.

## Limitations

- Risk Grade and Last Assessed are blank until at least one risk-bearing document has been processed for the vendor.
- Annual Value and Renewal Date populate from contract documents. If no contract has been uploaded, or the contract did not contain those fields, those columns remain empty.
- Vendor deletion is irreversible and removes all attached documents. There is no soft-delete or restore.
